{
  "name": "ddscope-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:unit": "vitest run tests/unit",
    "test:ui": "playwright test",
    "test:ui:setup": "playwright test --project=setup",
    "test:ui:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "@vitest/ui": "^2.1.8",
    "dotenv": "^16.4.5",
    "vitest": "^2.1.8"
  }
}
